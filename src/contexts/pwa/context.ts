import { createContext } from "react";
// import { IAuthAction, IAuthState } from "./types";

// type AuthContextType = [IAuthState, React.Dispatch<IAuthAction>];
export const PWAContext = createContext<any>(null);
